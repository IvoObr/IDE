const codeEditor = require('./codeEditor.css');
const terminal = require('./terminal.css');
const cssReset = require('./cssReset.css');
const header = require('./header.css');
const nav = require('./nav.css');

export {
    codeEditor,
    terminal,
    cssReset,
    header,
    nav
};